compilar: npx tsc

rodar o js: node dist/caminhoDesejado.js






//      PERGUNTA       SE FOR SIM (TRUE)      SE FOR NÃO (FALSE)
//         ↓                  ↓                       ↓
const status = this.lido ? "Lido ✅"    :    "Não lido ❌";

outro ex:
let idade = 18;
// Maior ou menor de idade?
let tipo = idade >= 18 ? "Maior de idade" : "Menor de idade";